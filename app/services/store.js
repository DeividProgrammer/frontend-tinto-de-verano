// Create a thin re-export so Ember Data's `store` service is available as expected.
// This matches the ember-data recommendation to avoid relying on legacy auto-install.
export { default } from 'ember-data/store';


